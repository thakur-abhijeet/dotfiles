# ─────────────────────────────────────────────
# Hyprland Monitor Configuration
# ─────────────────────────────────────────────
# Format:
#   monitor = <name>, <resolution@refresh>, <position>, <scale>
#
# Where:
# - <name>        = The connector name from `hyprctl monitors`
# - <resolution>  = Desired resolution, e.g., 1920x1080
# - @<refresh>    = Refresh rate (Hz) — same as shown by moitor tools
# - <position>    = XxY coordinates relative to virtual layout origin
# - <scale>       = UI scale factor (1 = 100%)
#
# See: Hyprland Monitor docs for detailed reference. :contentReference[oaicite:1]{index=1}

# ───────────── Standard Extended Setup ─────────────
monitor = HDMI-A-2, 1920x1080@100, 0x0, 1
monitor = eDP-1,   1920x1080@144, 1920x0, 1

# ─────────────────────────────────────────────
# Quick Reference (keep these handy)
# ─────────────────────────────────────────────

# 🔹 To use the monitor's preferred resolution:
# monitor = <name>, preferred, <position>, <scale>

# 🔹 To auto-position a new monitor to the right:
# monitor = <name>, preferred, auto, 1

# 🔹 Example placing second monitor *above* primary:
# monitor = <name2>, 1920x1080@60, 0x-1080, 1

# 🔹 Example placing second monitor *below* primary:
# monitor = <name2>, 1920x1080@60, 0x1080, 1

# 🔹 Negative Y moves display *up*, positive Y moves display *down*
# 🔹 Negative X moves display *left*, positive X moves display *right*
#   (Monitors use a top-left origin coordinate system.) :contentReference[oaicite:2]{index=2}

# ─────────────────────────────────────────────
# Scaling Tips
# ─────────────────────────────────────────────

# 📌 Scale factor ≠ percent of resolution; it controls UI scaling.
#    - 1   = 100% (normal)
#    - 1.25= 125% (slightly larger UI)
#    - 1.5 = 150% (HiDPI laptops)
#
#   If your UI looks too small on high dpi, try:
# monitor = eDP-1, 1920x1080@144, 1920x0, 1.5

# ➤ If scaling causes pointer misalignment,
#   adjust positions using the scaled size of the monitor. :contentReference[oaicite:3]{index=3}

# ─────────────────────────────────────────────
# Mirroring Example (advanced)
# ─────────────────────────────────────────────
# Note: Mirroring works best when both monitors share
#       the same native resolution. If different, use
#       "preferred" + "auto" scaling to avoid distortions. :contentReference[oaicite:4]{index=4}

# monitor = DP-1, 1920x1080@60, 0x0, 1, mirror, HDMI-A-1
# monitor = HDMI-A-1, preferred, auto, 1, mirror, DP-1

# ─────────────────────────────────────────────
# Useful Tools for Monitor Setup
# ─────────────────────────────────────────────
# 🛠 List connected monitors & get exact names:
#      hyprctl monitors
#
# 🛠 TUI tools:
#      hyprmon — terminal UI to arrange monitors visually :contentReference[oaicite:5]{index=5}
#
# 🛠 GUI tools:
#      nwg-displays — graphical monitor editor (save + apply) :contentReference[oaicite:6]{index=6}
