# ─────────────────────────────────────────────
# Starship Prompt Configuration
# ─────────────────────────────────────────────

"$schema" = "https://starship.rs/config-schema.json"

# ────────────── Overall Prompt Format ──────────────
# Added a space before username to avoid touching the terminal border
format = " $username $directory $git_branch $git_status$character"
add_newline = true

# ────────────── Username Module ──────────────
[username]
show_always = true
format = "[$user]($style)"
style_user = "bold red"

# ────────────── Directory Module ──────────────
[directory]
style = "bright-blue"
truncate_to_repo = true
home_symbol = "~"
format = "[$path]($style)"

# ────────────── Git Branch Module ──────────────
[git_branch]
symbol = " "
style = "bright-blue"
format = "($symbol$branch)"

# ────────────── Git Status Module ──────────────
[git_status]
style = "bold bright-blue"
format = "[$all_status]($style)"

# ────────────── Prompt Character Module ──────────────
[character]
success_symbol = "[❯](white bold)"
error_symbol   = "[✗](red bold)"
vicmd_symbol   = "[V](red bold)"

# ────────────── Time Module ──────────────
[time]
disabled = false
style = "white bold"
format = "[🕐 $time]($style)"
time_format = "%R"

# ────────────── Optional Enhancements ──────────────
# You can enable these modules if you want more info
# [battery]
# [jobs]
# [memory_usage]
